<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen  text-white bg-zinc-900 p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="mb-5 px-3 py-2 inline-block bg-red-500 rounded-md ">Logout</a>
        </div>
        <div class="flex items-start gap-3" >
             <div class="w-10 h-10 bg-red-500 rounded-md  overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.userImage %>" >
             </div>
        <h3 class="text-3xl mb-5">HELLO , <%= user.name %> 👋</h3>  </div>
        
        <h5 class="mb-5">You can create a post</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind ?."
                class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounden-md"> </textarea>
            <input class="px-3 py-2 w-40 text-sm bg-blue-500 block rounded-md mt-2" value="Create new post  "
                type="submit">
        </form>
        <div class="posts mt-20">
            <h5 class="text-2xl text-zinc-400"> Your Posts : </h5>
            <div class="postcontainer mt-5">
                <%  user.posts.reverse().forEach(function(post) {    %>
                <div class="post w-1/3 p-4 border-[1px] bg-zinc-800 border-zinc-800 mb-4">
                    <h4 class="mb-2 text-blue-500"  > @<%= user.username %></h4>
                    <p class="text-sm tracking-tight">
                        <%= post.content  %>
                    </p>
                    <small class="mt-2 inline-block " ><%= post.likes.length %> Likes</small>
                    <div class="btns gap-4 mt-5 flex mt-2">
                        <a href="/like/<%= post._id %> " class="text-blue-500"> 
                      
                         <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>   
                            </a>
                        <a href="/edit/<%= post._id %> " class="text-zinc-400"> Edit</a>
                    </div>
                </div>
          <%  }  )  %>
            </div>
        </div>

    </div>

</body>

</html>